---
description: Создать новый красивый блок/секцию на сайте в фирменном стиле
argument-hint: <что за блок, напр. "отзывы родителей" или "блок с гарантией возврата">
---

Создай новую секцию для сайта «Сказочник»: **$ARGUMENTS**

Действуй так:
1. Прочитай `CLAUDE.md` (фирменный стиль и правила).
2. Посмотри `index.html` — найди подходящее место для вставки и похожие по
   структуре блоки, чтобы переиспользовать классы и приёмы.
3. Свёрстай новый блок в едином стиле: фирменные CSS-переменные, скруглённые
   карточки, мягкие тени, адаптивность под мобильный.
4. Аккуратно встрой его в нужное место `index.html` (CSS — в `<style>`,
   JS при необходимости — в `<script>`).
5. Кратко на русском опиши, что добавил и куда.

Если непонятно, где разместить блок или какое должно быть содержимое —
сначала уточни у пользователя, потом верстай.
